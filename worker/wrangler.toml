name = "srt-academy-leads"
main = "src/index.js"
compatibility_date = "2025-01-01"

# Uncomment after deploy to enable per-IP rate limiting (5 req / 60s).
# See: https://developers.cloudflare.com/workers/runtime-apis/bindings/rate-limit/
# [[unsafe.bindings]]
# name = "RATE_LIMITER"
# type = "ratelimit"
# namespace_id = "1001"
# simple = { limit = 5, period = 60 }
